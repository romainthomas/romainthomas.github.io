var cfg_flat = svgPanZoom('#sub_ebdc', {
  zoomScaleSensitivity: 0.5,
  controlIconsEnabled: 1,
});
cfg_flat.zoom(1.5)


window.addEventListener('load', function () {
  Lightense('.lightense');
}, false);

